var github = $('.tab-content').find('.active').find('.github')

<% @git_actions.each do |action| %>
  $(github).prepend(
    $("<%= j render 'dashboard/github_feed_item', action: action %>")
      .hide()
      .fadeIn(3000)
    );
  $(github).children().last().fadeOut(3000).remove();
  // Add slide down functionality
<% end %>

GitPoller.poll();